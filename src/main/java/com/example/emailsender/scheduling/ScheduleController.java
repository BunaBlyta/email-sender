package com.example.emailsender.scheduling;

import org.springframework.stereotype.Controller;

@Controller
public class ScheduleController {
    // TODO: Handle GET /scheduled (list scheduled messages), POST /scheduled/cancel/{id}
}
