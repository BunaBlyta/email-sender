package com.example.emailsender.inbox;

import org.springframework.stereotype.Controller;

@Controller
public class InboxController {
    // TODO: Handle GET /inbox (list threads), GET /inbox/thread/{id} (view thread)
}
