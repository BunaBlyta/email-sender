package com.example.emailsender.tracking;

import org.springframework.stereotype.Controller;

@Controller
public class ReadReceiptController {
    // TODO: Handle GET /track/{trackingId} — serve 1x1 pixel and record read receipt
}
