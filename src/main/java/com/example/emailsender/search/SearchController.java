package com.example.emailsender.search;

import org.springframework.stereotype.Controller;

@Controller
public class SearchController {
    // TODO: Handle GET /search?q=... — render search results page
}
